//! mulberry32, ported exactly (PARITY_TESTING.md)
//!
//! Phase 0 walking skeleton: no engine logic yet (ROADMAP.md). Ported in
//! pipeline order starting Phase 1 (MVP_SCOPE.md).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles_and_tests_run() {
        assert_eq!(2 + 2, 4);
    }
}
